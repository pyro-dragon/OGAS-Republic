import {GameObject} from "../GameObject";

// A logic board that can contain logic components and their connections
export class LogicBoard extends GameObject {
    constructor() {
        super();
    }

    Update(deltaTime: number){

        // Update components
    }
}